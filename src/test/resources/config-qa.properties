# Base URL for QA environment (example, still using Google for now)
base.url=https://www.google.com

browser=chrome
headless=true
implicit.wait=5
explicit.wait=10
page.load.timeout=20
retry.enabled=true
retry.count=2
max.parallel.threads=5
